{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "nodemon src/server.ts",
    "docker:up": "docker-compose up -d --build",
    "docker:down": "docker-compose down",
    "prisma:migrate": "docker-compose exec backend npx prisma migrate dev",
    "prisma:pull": "docker-compose exec backend npx prisma db pull",
    "prisma:push": "docker-compose exec backend npx prisma db push",
    "prisma:generate": "docker-compose exec backend npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.12.0",
    "@types/cors": "^2.8.17",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "firebase": "^10.10.0",
    "firebase-admin": "^12.0.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.11",
    "@types/node": "^20.12.2",
    "nodemon": "^3.1.7",
    "prisma": "^5.12.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
